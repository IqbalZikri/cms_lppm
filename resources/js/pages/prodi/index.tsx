import { PaginatedData } from "@/interface/pagination";

interface Prodi {
    id: number;
    fakultas_id: number;
    kode_prodi: number;
    nama_prodi: string;
}

export default function Prodi({ data }: PaginatedData<Prodi>) {
    const [open, ]
};
